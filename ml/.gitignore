data/raw/
data/labelled/*.jsonl
!data/labelled/demo_labeled.jsonl
data/audits/*.jsonl
notebooks/.ipynb_checkpoints/
__pycache__/
*.pyc
# Notebook-generated model artifacts (joblib bundles + embedding caches) and
# metrics are tracked in the repo so the trained models ship with the project.
